#!/usr/bin/env node
process.env.NODE_ENV = 'test';
require('../rsync');

var Mocha = require('mocha');


var mocha = new Mocha({ reporter: 'spec'});

mocha.addFile('test/args.test.js');
mocha.run(function (failures) {
    process.exit(failures);
});
